web: apt-get update && apt-get install -y python3 python3-pip python3-venv && python3 -m venv venv && . venv/bin/activate && pip install -r requirements.txt && python -m uvicorn api:app --host 0.0.0.0 --port $PORT
